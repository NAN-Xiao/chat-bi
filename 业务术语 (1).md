# BI 业务术语知识库

## 1. 新增用户

```yaml
term: 新增用户
aliases: [注册用户, 新增注册用户, New Users]
definition: 指定窗口内首次满足注册或开户条件的去重主体数。
formula: "COUNT(DISTINCT subject_id)"
event: UserRegister
constraints:
  - 注册日期使用注册事件或开户事实的业务时间，不能使用后续活跃日期替代。
  - 注册事件名、主体字段和首次判定规则必须来自当前工作空间配置。
sql_example: null
```

## 2. DAU
-1123
```yaml
term: DAU
aliases: [日活, 日活跃用户数, Daily Active Users]
definition: 一个自然日内发生有效活跃行为的去重主体数。
formula: "COUNT(DISTINCT subject_id)"
event: UserActive
constraints:
  - 有效活跃事件、主体字段和日期字段必须来自当前工作空间定义。
  - 事件次数不是 DAU；同一主体一天多次行为只计一次。
  - 下方示例仅适用于 flam 工作空间 datasource_id=3。
sql_example:
  question: flam 工作空间指定看板日期范围内每天的 DAU 是多少？
  sql: |
    SELECT
        STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d') AS `日期`,
        COUNT(DISTINCT e.uid) AS `DAU`
    FROM `event` AS e
    WHERE e.dt BETWEEN {{dashboard_start_yyyymmdd}} AND {{dashboard_end_yyyymmdd}}
      AND e.prod = {{prod_id}}
      AND e.event = 'UserActive'
    GROUP BY e.dt
    ORDER BY e.dt;
```

## 3. WAU

```yaml
term: WAU
aliases: [周活, 周活跃用户数, Weekly Active Users]
definition: 一个自然周内发生有效活跃行为的去重主体数。
formula: "COUNT(DISTINCT subject_id) BY calendar_week"
event: UserActive
constraints:
  - 必须在自然周范围内整体去重，不能把每日 DAU 相加得到 WAU。
  - 周起始日和不完整首尾周的处理必须遵循当前工作空间日期 Skill。
  - 下方示例仅适用于修仙工作空间 datasource_id=6。
sql_example:
  question: 修仙工作空间指定日期范围内每周的 WAU 是多少？
  sql: |
    SELECT
        DATE_SUB(
            STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d'),
            INTERVAL WEEKDAY(STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d')) DAY
        ) AS `周`,
        COUNT(DISTINCT e.uid) AS `WAU`
    FROM `event` AS e
    WHERE e.dt BETWEEN {{dashboard_start_yyyymmdd}} AND {{dashboard_end_yyyymmdd}}
      AND e.prod = {{prod_id}}
      AND e.event = 'UserActive'
    GROUP BY DATE_SUB(
        STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d'),
        INTERVAL WEEKDAY(STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d')) DAY
    )
    ORDER BY DATE_SUB(
        STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d'),
        INTERVAL WEEKDAY(STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d')) DAY
    );
```

## 4. MAU

```yaml
term: MAU
aliases: [月活, 月活跃用户数, Monthly Active Users]
definition: 一个自然月内发生有效活跃行为的去重主体数。
formula: "COUNT(DISTINCT subject_id) BY calendar_month"
event: UserActive
constraints:
  - 必须在自然月范围内整体去重，不能把每日 DAU 相加得到 MAU。
  - 自然月定义和不完整首尾月的处理必须遵循当前工作空间日期 Skill。
sql_example: null
```

## 5. 收入

```yaml
term: 收入
aliases: [流水, 付费金额, Revenue]
definition: 指定窗口内成功有效交易金额的总和。
formula: "SUM(valid_payment_amount)"
event: ServerPayLog 或当前工作空间定义的成功支付事件
constraints:
  - 成功状态、退款冲正、金额字段、币种和支付事件必须来自当前工作空间配置。
  - 不能用用户累计快照代替交易明细金额，不能因一对多关联重复计算金额。
sql_example: null
```

## 6. 付费用户

```yaml
term: 付费用户
aliases: [付费人数, 付费主体数, Paying Users]
definition: 指定窗口内发生成功有效付费行为的去重主体数。
formula: "COUNT(DISTINCT paying_subject_id)"
event: ServerPayLog 或当前工作空间定义的成功支付事件
constraints:
  - 只能使用成功有效付费主体的去重数。
  - 事件次数、订单数和付费用户数是不同指标，不能互相替代。
sql_example: null
```

## 7. ARPU

```yaml
term: ARPU
aliases: [每用户平均收入, 用户人均收入, Average Revenue Per User]
definition: 指定收入除以指定用户分母；最终分母由当前工作空间确定。
formula: "ARPU = specified_revenue / specified_user_denominator"
event: 当前工作空间定义的收入事件与用户分母事件
constraints:
  - 不得默认分母；分母为 0 时返回 NULL，不能平均不同粒度的 ARPU。
sql_example:
  question: 修仙工作空间每日 ARPU 是多少？
  sql: |
    WITH daily_pay AS (
        SELECT e.dt,
               SUM(CAST(NULLIF(JSON_UNQUOTE(JSON_EXTRACT(e.personal, '$.money')), '') AS DECIMAL(18,4))) AS amount
        FROM `event` AS e
        WHERE e.dt BETWEEN {{dashboard_start_yyyymmdd}} AND {{dashboard_end_yyyymmdd}}
          AND e.prod = {{prod_id}} AND e.event = 'ServerPayLog'
        GROUP BY e.dt
    ), daily_active AS (
        SELECT e.dt, COUNT(DISTINCT e.uid) users
        FROM `event` AS e
        WHERE e.dt BETWEEN {{dashboard_start_yyyymmdd}} AND {{dashboard_end_yyyymmdd}}
          AND e.prod = {{prod_id}} AND e.event = 'UserActive'
        GROUP BY e.dt
    )
    SELECT STR_TO_DATE(CAST(d.dt AS CHAR), '%Y%m%d') AS `日期`,
           ROUND(COALESCE(p.amount, 0) / NULLIF(d.users, 0), 2) AS `ARPU`
    FROM daily_active AS d
    LEFT JOIN daily_pay AS p ON p.dt = d.dt
    ORDER BY d.dt;
```

## 8. ARPPU

```yaml
term: ARPPU
aliases: [每付费用户平均收入, 付费用户人均收入, Average Revenue Per Paying User]
definition: 指定成功有效收入除以同期成功有效付费用户数。
formula: "ARPPU = valid_revenue / paying_users"
event: 当前工作空间定义的成功支付事件
constraints:
  - 分母只能使用成功有效付费主体的去重数。
  - ARPPU 与客单价不同；客单价通常以有效订单数为分母。
  - 下方示例仅适用于修仙工作空间 datasource_id=6。
sql_example:
  question: 修仙工作空间指定日期范围内每天的 ARPPU 是多少？
  sql: |
    SELECT
        STR_TO_DATE(CAST(e.dt AS CHAR), '%Y%m%d') AS `日期`,
        ROUND(
            SUM(CAST(NULLIF(JSON_UNQUOTE(JSON_EXTRACT(e.personal, '$.money')), '') AS DECIMAL(18, 4)))
            / NULLIF(COUNT(DISTINCT e.uid), 0),
            2
        ) AS `ARPPU`
    FROM `event` AS e
    WHERE e.dt BETWEEN {{dashboard_start_yyyymmdd}} AND {{dashboard_end_yyyymmdd}}
      AND e.prod = {{prod_id}}
      AND e.event = 'ServerPayLog'
    GROUP BY e.dt
    ORDER BY e.dt;
```

## 9. 付费率

```yaml
term: 付费率
aliases: [付费渗透率, Payer Rate, Paying Penetration]
definition: 指定窗口内付费主体数除以明确的目标主体分母。
formula: "payer_rate_pct = paying_users / denominator_users * 100"
event: 当前工作空间定义的支付事件与分母事件
constraints:
  - 必须明确分子、分母和统计窗口；分母可能是活跃、新增、曝光或试用主体。
  - 日付费率、累计付费转化率和生命周期日付费率不能混称。
sql_example: null
```

## 10. 留存率

```yaml
term: 留存率
aliases: [次日留存, D1 留存, D3 留存, D7 留存, Cohort Retention]
definition: 同一注册 cohort 在精确生命周期日再次活跃的主体数占成熟 cohort 主体数的比例。
formula: "Dn_retention_pct = retained_users_on_Dn / mature_cohort_users * 100"
event: UserRegister 与 UserActive
constraints:
  - 分母固定；Dn 是注册日加 n 个自然日的精确日期，未成熟 cohort 不进入分母。
sql_example:
  question: flam 工作空间每日新增用户的 D1 留存率是多少？
  sql: |
    SELECT STR_TO_DATE(CAST(r.dt AS CHAR),'%Y%m%d') AS `注册日期`,
           COUNT(DISTINCT r.uid) AS `新增用户数`,
           COUNT(DISTINCT a.uid) AS `D1留存用户数`,
           ROUND(COUNT(DISTINCT a.uid) / NULLIF(COUNT(DISTINCT r.uid),0) * 100,2) AS `D1留存率`
    FROM `event` AS r
    LEFT JOIN `event` AS a
      ON a.uid = r.uid
     AND a.dt = CAST(DATE_FORMAT(DATE_ADD(
         STR_TO_DATE(CAST(r.dt AS CHAR),'%Y%m%d'),INTERVAL 1 DAY
     ),'%Y%m%d') AS UNSIGNED)
     AND a.prod = {{prod_id}} AND a.event = 'UserActive'
    WHERE r.dt BETWEEN {{dashboard_start_yyyymmdd}} AND CAST(DATE_FORMAT(DATE_SUB(
        STR_TO_DATE(CAST({{dashboard_end_yyyymmdd}} AS CHAR),'%Y%m%d'),INTERVAL 1 DAY
    ),'%Y%m%d') AS UNSIGNED)
      AND r.prod = {{prod_id}} AND r.event = 'UserRegister'
    GROUP BY r.dt
    ORDER BY r.dt;
```

## 11. LTV

```yaml
term: LTV
aliases: [生命周期价值, 用户生命周期价值, Lifetime Value]
definition: 同一 cohort 截至生命周期日 n 的累计有效收入除以固定 cohort 主体数。
formula: "LTV(n) = cumulative_valid_revenue_of_cohort_by_day_n / cohort_users"
event: UserRegister 与当前工作空间定义的收入事件
constraints:
  - cohort 和分母在所有生命周期日保持不变。
  - 累计 LTV 理论上不下降；未成熟生命周期日不能按 0 处理。
sql_example: null
```

## 12. 转化率与漏斗

```yaml
term: 转化率与漏斗
aliases: [漏斗转化, 步骤转化率, Conversion Rate, Funnel]
definition: 同一主体、同一窗口和明确步骤顺序下，当前步骤人数相对于起始步骤或前一步人数的比例。
formula: "start_conversion_pct = step_users / start_users * 100; prev_conversion_pct = step_users / previous_step_users * 100"
event: 当前工作空间事件字典中定义的漏斗步骤事件
constraints:
  - 每一步先构造主体级完成状态，不能直接用事件行数拼接漏斗。
  - 步骤、顺序和完成条件必须来自当前工作空间事件字典。
sql_example: null
```

## 13. ROI

```yaml
term: ROI
aliases: [投资回报率, 投放回报, Return on Investment]
definition: 在明确成本、收益口径和归因窗口后，收益相对于成本的回报比例。
formula: "ROI = attributed_return / attributed_cost"
event: 当前工作空间定义的投放成本事件与收益事件
constraints:
  - 必须说明采用的 ROI 公式、成本口径和收益回收窗口。
  - 不得直接比较不同归因窗口或不同币种的 ROI。
sql_example: null
```

## 14. 客单价

```yaml
term: 客单价
aliases: [平均订单金额, Average Order Value, AOV]
definition: 指定窗口内有效订单金额除以有效订单数。
formula: "AOV = valid_order_amount / valid_order_count"
event: 当前工作空间定义的成功订单事件
constraints:
  - 客单价的分母是订单数，不是付费用户数；后者对应 ARPPU。
  - 退款、拆单和币种转换必须按当前工作空间订单 Skill 处理。
sql_example: null
```

## 15. 流失与回流

```yaml
term: 流失与回流
aliases: [流失率, 沉默用户, 回流用户, Churn, Reactivation]
definition: 流失表示主体在观察窗口内从活跃转为不活跃；回流表示沉默一段时间后重新满足活跃条件。
formula: "churn_rate = churned_users / eligible_users * 100; reactivation_rate = reactivated_users / dormant_users * 100"
event: 当前工作空间定义的活跃、沉默和回流事件
constraints:
  - 沉默天数、观察窗口、活跃事件和主体边界必须由当前工作空间 Skill 定义。
  - 流失、未活跃和未成熟 cohort 不能互相替代。
sql_example: null
```
