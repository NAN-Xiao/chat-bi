# 图表配置 SQL 浏览器验证记录

日期：2026-09-16。工作树：`D:/AIWork6/release_1.3.0`，分支：`release/release_1.3.0`。

通过本地页面 `http://127.0.0.1:5173/` 实际点击配置控件、提交生成并查看图表和数据预览。沿用已登录用户当前的 `gig` 工作空间及绑定数据源 12，日期为页面选择的 2026-09-09 至 2026-09-15。未切换到其他工作空间。

## 本地服务

两次重启前均拉取当前分支，结果均为 Already up to date，保留本任务未提交修改。

| 服务 | 最终 PID | 验证 |
|---|---:|---|
| 前端 5173 | 32280 | HTTP 200 |
| API 8000 | 59860 | 健康检查 HTTP 401，业务接口正常 |
| MCP 8001 | 77716 | 监听正常，根路径 HTTP 404 |
| Worker | 85904 | 单个进程运行 |

API、MCP、Worker 队列均为 `local-DONGJINCHAO-release_1.3.0`。LLM 配置核对为 `120 / 900 / 1`。未操作远程服务、数据库或 Redis 的启停。

## 最终测试结果

| 浏览器配置 | SQL/预览 | LLM 调用 | 独立数据核对 |
|---|---|---:|---|
| 游戏启动，总次数 | 成功，7 行 | 1 | 逐日 168、116、164、112、150、104、96，合计 910；与独立 COUNT 查询一致 |
| 游戏启动，用户 ID 去重数 | 成功，7 行 | 1 | 逐日 15、14、16、11、11、12、13；与独立 COUNT(DISTINCT uid) 一致 |
| 游戏启动总次数，用户 ID IS NOT NULL，按用户 ID 分组 | 修正后成功，238 行 | 1 | 34 个用户 × 7 天；92 个非零组合、146 个补零组合，所有键和值逐项一致，合计 910 |

LLM 次数指 SQL 生成/修复，不含向量检索。上述最终成功请求均无网络重试。用户 ID IS NOT NULL 保留数据库中的空字符串值，不自行改写为非空字符串口径。

请求 ID：

- 总次数：`6ea534353158463b91b521cde880ada0`，生成约 35.1 秒，预览约 1.2 秒。
- 去重数：`3b8fdfde6772425eafd297735f2835ab`，生成约 40.9 秒，预览约 1.2 秒。
- 修正后分组：`cb812c1bac9942e88dca482c474893d3`，生成约 102.6 秒，预览约 3.7 秒。

## 本轮发现并修正的问题

首次分组测试请求 `317dbe0265f14b798cfb3440a65bc91d` 使用了 4 次 LLM，持续约 207 秒。前端没有在 180 秒提前超时，但测试暴露了两个问题：

1. 通用校验把合法 ORDER BY 唯一输出别名误判为连接来源歧义。修正别名解析规则，继续拦截真实歧义，并用实际数据源的小型 SELECT 验证。
2. 分组维度域遗漏所选事件条件，结果扩展到 129,535 行，其中绝大部分是不相关零值组合，页面预览被截为 10,000 行。增加配置驱动的维度域规则及每条来源分支的校验后，结果恢复为完整、准确的 238 行。

本轮合计进行了 4 个浏览器生成请求，SQL 生成/修复合计调用 LLM 7 次。

修正后重新启动全部四个本地服务，并从浏览器重新提交相同分组配置。655 项相关后端回归测试通过。未将测试配置保存到原看板。

## 证据与范围

- `group-generated.sql`：浏览器最终生成并实际执行的参数化 SQL。
- `group-comparison.json`：与独立事实查询逐项比较的结果。
- `group-comparison-before-fix.json`：修正前的结果膨胀记录。
- 应用日志：`backend/logs/info.log`，可按上述请求 ID 定位。

本次浏览器覆盖事件分析的总次数、去重人数、分组及筛选组合；未逐一操作其他分析模型，也未验证保存/共享/复制看板流程。SQL 实际执行与结果显示均已验证，未仅依赖 HTTP 200 或 EXPLAIN。
