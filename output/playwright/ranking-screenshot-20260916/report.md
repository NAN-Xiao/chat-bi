# 截图排行榜配置复测：FAIL

2026-09-16，在本地浏览器按用户截图配置并执行。使用已授权工作空间 gig / 数据源12，沿用2026-08-19至2026-09-15的28天业务范围。没有加载工作空间Data Skill。

| 配置 | 实际选择 |
|---|---|
| 主体 | event.uid（当前页面显示名为“用户ID”，对应截图UID） |
| 排行主指标 | 后端充值 ServerPayLog，金额 personal.money 求和，降序 |
| 并列处理 | 按默认排序 |
| 同时展示指标 | 别名“活跃”，当日活跃 UserActive，总次数 |
| 同时展示属性 | 媒体来源 adinfo.mediaSource |
| 全局筛选/分组 | 无额外配置；保留数据源授权的产品范围 |

## 实测结果

SQL生成、校验、执行和页面表格均完成，但结果语义未通过。

- 充值主指标范围只有3个UID、3条付款，独立金额合计13.00。
- 最终SQL返回204行，其中201个UID只因“同时展示”的活跃事件进入排行榜，充值金额为0。
- “同时展示”本应补充主指标已有主体的信息。当前SQL先把充值与活跃事件合并为scoped_events，再按这个并集的全部UID聚合和排名，扩大了主指标的主体集合。
- 3个实际充值UID没有丢失或重复，主指标金额及活跃总次数与独立查询相符。

| 正确充值主体的名次 | 金额 | 活跃总次数 | 媒体来源 |
|---|---:|---:|---|
| 1 | 6 | 14 | tencent |
| 2 | 6 | 2 | Organic |
| 3 | 1 | 7 | tencent |

另一个生成规则缺口：修复后的SQL用 `ROW_NUMBER() OVER (PARTITION BY uid ORDER BY dt DESC)` 为媒体来源取一行。配置没有指定“最近一次”策略，且同一天内没有稳定的排序键。本次3个充值UID的媒体来源单值且核对一致，因此未把此项描述为已经观察到的属性值错误。

## 调用链与根因

请求ID：`c8b86ef8e1614cf79cdf5427ff89773b`。

1. 首次生成用MAX(media_source)取媒体来源，被现有属性校验拦截。
2. 第二次LLM修复改用按dt倒序选一行，现有校验通过。
3. SQL预览实际执行成功，204行、5列，执行耗时约1300ms；独立明细核对确认201个额外主体。

本次共2次逻辑LLM调用、2次HTTP尝试、0次网络重试；生成加修复约131.4秒。

`_ranking_sql_result_issues`当前检查固定别名、是否出现聚合/排名函数及属性是否使用禁止聚合，但没有验证最终主体集合由主指标事件决定，也未验证属性选行策略是否来自显式配置。问题属于排行榜生成和语义校验覆盖不足，不能用HTTP成功或SQL可执行判为正确。

排行榜现有自动化验证：`python -m pytest tests/test_dashboard_ai_sql_generator.py -k ranking -q --disable-warnings`，11 passed、241 deselected。这些用例尚未覆盖本次“充值金额＋同时展示活跃＋媒体来源”的主体范围不变性。

后续修复应在通用排行榜契约层，以主指标筛选和聚合产生主体集合，附加指标分别按主体聚合再关联，并验证它们不会扩展或放大主集合；属性的多值选取必须有明确策略，不能由模型临时选择MAX、最近记录或任意一条。

## 证据与状态

- 最终SQL：`c8b86ef8e1614cf79cdf5427ff89773b.sql`
- 独立核对：`c8b86ef8e1614cf79cdf5427ff89773b.json`
- 当前浏览器保留复现配置和执行结果；该失败用例没有作为通过的图表保存到测试看板。
- 本轮仅测试和定位，未修改业务源码、Data Skill或业务明细。
- 工作树：D:/AIWork6/release_1.3.0；分支：release/release_1.3.0。

此前报告的排行榜PASS仅代表“启动次数＋无附加指标/属性”基础用例，不覆盖本次复合配置。
