# 检查记录

## 实施结果

- V2 与 LEGACY 知识库页提供“平台知识库 / 工作空间知识库”范围下拉；工作空间范围显示独立工作空间下拉。
- 平台管理员可选择任意启用且非默认平台租户的工作空间；工作空间管理员和普通成员的工作空间选择固定为当前工作空间。
- 后端 V2/LEGACY 列表与创建接口按授权 `tenant_id` 过滤和创建；跨工作空间访问返回 403，平台范围携带工作空间参数返回 400，无效/停用/默认平台租户返回 404。
- 平台管理员可管理平台和所选工作空间知识；工作空间管理员只能管理当前工作空间知识；普通成员两类知识均为只读。
- 普通成员可进入知识库管理路由；新建、编辑、发布、回滚和归档入口继续由角色与接口 `can_manage` 双重控制。

## 运行与浏览器验收

- 使用当前工作区代码在隔离端口启动 API `8010` 与前端 `5179`，未接管已有 `5173/8000/8001`；验收后已关闭临时进程。
- 启动配置核对：`LLM_REQUEST_TIMEOUT=120`、`LLM_TASK_MAX_WAIT_SECONDS=900`、`LLM_MAX_RETRIES=1`。
- 平台管理员 `admin`：平台知识可管理；切换“修仙”和“flam”后列表分别返回对应工作空间数据，工作空间知识显示新建/编辑/归档入口。
- 工作空间管理员 `dongjinchao_t1`：当前“修仙”工作空间选择禁用；工作空间知识可新建/编辑/归档；切到平台知识后无写入口。
- 普通成员 `djc`：可直接进入知识库页；当前工作空间选择禁用；平台和工作空间范围均无新建/编辑/归档入口。
- 桌面 `1440x900`：平台壳层与工作空间壳层均无页面横向溢出；工具栏 `scrollWidth == clientWidth`，双下拉与操作按钮无重叠。
- 移动 `390x844`：发现工作空间顶部导航与 240px 管理侧栏导致根页面溢出；已将窄屏工作空间管理侧栏压缩为 64px 图标栏并收紧顶栏/内容间距。修复后内容宽 326px，`document.scrollWidth == clientWidth == 390`，双下拉与操作按钮完整显示。
- 原有 `5177` 当前工作区 Vite 进程保留；重复 `5178` 已关闭。

## 自动化检查

- 后端定向测试：`22 passed`。
- 后端 Ruff：通过。
- 前端知识库契约测试：`9 passed`。
- `npm run build`：通过；仅有仓库既有 Rollup 动态导入和大 chunk 警告。
- `git diff --check`：通过；仅有 Git 的 LF/CRLF 行尾提示。

## Trellis 复核修复

- 复核发现 V2 平台管理员可在创建工作空间知识时省略 `tenant_id`，列表缺少选择时也没有统一返回显式校验错误；这会把“必须明确选择工作空间”的产品约束留给无效租户兜底。
- V2 与 LEGACY 工作空间范围现在都要求平台管理员显式传入 `tenant_id`；缺失时返回 400。新增列表与创建回归测试，防止平台知识租户被误当作工作空间上下文。

## 已知非本任务失败

- `backend/tests/test_knowledge_base_management_api.py` 仍有两个既有问题：测试替身 `_IncludedRouter` 缺少 `.path`；能力测试期望 `LEGACY`，当前运行时返回 `UPGRADING`。本任务未修改这些不相关契约。

## 用户反馈后的导航修正

- 用户发布环境截图显示左侧“知识库管理”仍是单一菜单项；上一轮把“双下拉”只实现和验收成页面范围/工作空间筛选，遗漏了用户期望的侧栏父子菜单层级。
- `/system/knowledge-base` 现在重定向到 `/system/knowledge-base/platform`，并展开“平台知识库”和“工作空间知识库”两个子菜单；子路由元数据驱动页面范围，页面范围选择器切换时同步导航。
- 前端知识库契约测试 `9 passed`，`npm run build` 通过，`git diff --check` 通过（仅 LF/CRLF 提示）。
- 浏览器确认旧入口会重定向到新平台子路由；当前内置浏览器无本地登录态且 Chrome 会话未连接，因此未完成登录后侧栏展开截图。菜单组件会根据路由 `children` 渲染 `ElSubMenu`，契约测试直接覆盖两个子路由及范围导航映射。

## Bug Analysis: 知识库菜单层级验收遗漏

- 根因类别：E 隐式假设 + D 测试覆盖缺口。将“两个下拉列表”解释为页面筛选控件，没有结合用户标注的侧栏位置确认导航层级。
- 预防：管理导航需求的浏览器验收必须同时确认父菜单展开、子项文案、子项路由和落地页状态；不能用页面内等价筛选代替侧栏层级验收。
- 固化：已更新 `.trellis/spec/frontend/project-runtime.md`，并新增路由/范围同步契约测试。

## 用户反馈后的重复菜单修正

- 用户截图中的第二个同名“知识库管理”不是业务路由，而是通用子菜单组件自动渲染的 popup 标题行。
- 知识库父路由增加 `hidePopupTitle: true` 后，浏览器实际刷新 `/system/knowledge-base/workspace` 验证：侧栏只显示一个“知识库管理”父菜单，子项为“平台知识库”和“工作空间知识库”，重复中间行已消失。
- 桌面实际页面无横向溢出；移动 `390x844` 下两个子项仍完整存在，根页面 `scrollWidth == clientWidth == 390`。
- 回归测试 `5 passed`，`npm run build` 通过，`git diff --check` 通过。
