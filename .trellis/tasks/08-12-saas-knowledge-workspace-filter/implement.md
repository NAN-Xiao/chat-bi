# 执行与验证

1. 更新知识库 API 类型、平台管理员工作空间过滤和三档角色的详情/写操作授权。
2. 更新 V2/LEGACY 知识库页面的范围下拉、工作空间下拉、只读状态和请求上下文。
3. 放开普通成员的知识库只读路由入口，保留其他系统管理页的管理员限制。
4. 增加后端权限/列表回归测试与前端角色矩阵/筛选控件测试。
5. 运行相关测试、前端构建和实际 API 健康检查。
6. 重启或确认实际服务与当前工作区版本一致，使用浏览器验证三档角色、切换、详情和横向溢出。

## 实施日志

- 完成 V2/LEGACY 双下拉、三档角色写权限、平台管理员跨工作空间后端过滤与创建上下文。
- 增加跨工作空间 403、平台范围错误参数 400、无效工作空间 404 等回归测试。
- 浏览器验收发现工作空间管理壳层在 390px 下根页面横向溢出；补充 64px 移动图标侧栏和顶部导航收紧规则，并增加布局契约测试。
- 完整检查与真实三角色验收结果见 `check.md`。
- 用户在发布环境截图中指出左侧“知识库管理”仍是单一菜单项。已补充平台/工作空间两个子路由菜单，并让页面范围选择器与子路由保持同步。
- 用户随后发现父菜单展开后多出一个同名“知识库管理”中间行。根因是通用 `MenuItem` 会为子菜单默认插入 popup 标题；已在知识库父路由设置 `hidePopupTitle: true`，仅隐藏重复标题，保留父菜单及两个子项。
