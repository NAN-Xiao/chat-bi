# 执行计划

1. 调整默认工作空间解析函数和 `resolve_current_tenant()` 调用关系。
2. 保持前端当前上下文同步逻辑不变，确认不引入第一条盲选。
3. 增加后端单元测试，覆盖默认空间及显式权限边界。
4. 运行后端定向测试、前端相关静态测试和构建检查。

## 实际结果

- 已恢复未指定工作空间时按 `is_primary` 成员关系解析默认上下文。
- 已保留显式工作空间 ID 的严格权限校验，不再静默回退。
- 后端工作空间相关定向测试：19 passed。
- Ruff 检查通过。
- 前端 `vue-tsc` / build 受工作树依赖解析和现有 TypeScript 配置问题阻断，且检查过程生成的未跟踪 `.js` 文件已清理。
