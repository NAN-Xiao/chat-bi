# 检查记录

## 根因与修复

- 根因：版本历史只追加不裁剪，版本列表自然返回全部历史；发布任务、派生检索数据和源文件引用也随旧版本持续保留。
- 修复：新增租户隔离的物理裁剪方法，保护当前/草稿/发布中指针及活动发布任务版本，按版本号补足最近 10 个。
- 删除顺序：先删除候选版本发布任务，再删除版本；切片、适用性和引用投影使用数据库级联。
- 文件安全：API 提交数据库事务后才执行全局引用检查；共享文件保留，文件 I/O 失败记录日志但不伪装成版本创建失败。

## 自动验证

- `pytest` 知识库测试：`180 passed, 7 skipped`。
- 聚焦新增/文件测试：`20 passed`。
- Ruff：通过。
- Python `compileall`：通过。
- 前端知识库 Node 测试：`20 passed`。
- 前端 `npm run build`：通过；仅有现存的动态导入和大 chunk 警告。
- `git diff --check`：通过。
- Mypy：共享虚拟环境的 `mypy 1.20.2` 编译模块与包文件不匹配，启动时报 `No module named '0aca9ce3d91742c5b361__mypyc'`；本轮未修改依赖环境，以 Ruff、运行测试和编译检查覆盖本次 Python 变更。

## 真实运行验证

- 隔离前端 `5175`：HTTP 200。
- 隔离 API `8100`：登录方式接口 HTTP 401，服务在线。
- 隔离 MCP `8101`：根路径 HTTP 404，服务在线。
- 隔离 Worker：日志确认队列 `local-retain-latest-10-kb-versions`，不是 `default`。
- LLM 配置：`120 900 1`。
- PostgreSQL 回滚事务：构造 11 个版本、一个旧发布任务和一个旧切片后执行裁剪，结果为版本 10、任务 0、切片 0；事务整体回滚，无测试数据残留。
- 验证完成后已停止 5175/8100/8101 和隔离 Worker，未影响原有 5173/5174/8000/8001 服务。

## 质量结论

- PRD 验收项已覆盖，没有发现需要继续修复的功能问题。
- 后端规范已增加知识库版本保留契约，明确禁止用前端切片替代物理清理。
